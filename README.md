```
PIOA-M6O-122BV-25/
│
├── src/
│ └── db/
│   └── backend/
│   │  ├── __init__.py
│   │  └── memory.py
│   │
│   ├── __init__.py
│   ├── __main__.py
│   └── tui.py
│
├── tests/
| ├── __init__.py
│ ├── test_memory.py
│ ├── test_tui.py
│ ├── test_csv.py
│ ├── test_file.py
│ └── test_indexed_memory.py
│
└── README.md
```

- `src/db/__main__.py` – запускает приложение.
- `src/db/tui.py` – содержит все меню, ввод/вывод, взаимодействие с пользователем.
- `src/db/backend/memory.py` – реализует хранение таблиц в словарях Python, управление таблицами и операции создания, чтения, обновления, удаления записей.
- `src/tests/test_memory.py` - хранит набор тестов для БД
- `src/tests/test_tui.py` - хранит набор тестов для пользовательского интерфейса
- `src/tests/test_csv.py` - хранит набор тестов для CSV
- `src/tests/test_file.py` - хранит набор тестов для JSON
- `src/tests/test_indexed_memory.py` - хранит набор тестов для индексации


## Функциональность

Проект поддерживает:

- **Создание и удаление нескольких таблиц** (одна таблица по умолчанию – `cars`).
- **Полный набор CRUD операций** для любой таблицы:
  - **Create** – добавление новой записи.
  - **Read** – чтение записей с фильтрацией по одному или нескольким полям.
  - **Update** – обновление записей, соответствующих фильтру.
  - **Delete** – удаление записей, соответствующих фильтру (с подтверждением).
  - **Sort** – сортировка таблиц по заданным критериям (с подтверждением).
- **Фильтрация** при поиске, обновлении и удалении по любым полям (brand, price, color). Пустые поля игнорируются.
- **Обработка ошибок**:
  - Некорректный ввод (не число в поле price, пустые строки).
  - Обращение к несуществующей таблице или записи.
  - Попытка создать таблицу с уже существующим именем.

- **Консольный интерфейс** с пошаговыми меню и выводом записей в виде таблицы (реализованным в виде класса CarDatabaseUI).


## Способы хранения:

- **In‑memory** – данные живут только в оперативной памяти (теряются после выхода).
- **Файловая БД (JSON)** – каждая таблица сохраняется в отдельный JSON‑файл в папке `data/`.
- **Файловая БД (CSV)** – таблицы хранятся в CSV‑файлах (папка `csv_data/`) + метаданные в JSON.
- **Индексированная in‑memory БД** – ускоряет поиск по индексированным полям.

## Пример таблицы «cars» 

| Поле    | Тип    | Описание          |
|---------|--------|-------------------|
| id      | int    | уникальный номер (генерируется автоматически) |
| brand   | str    | марка автомобиля   |
| price   | int    | цена в руб.        |
| color   | str    | цвет               |


## Запуск тестирования
    pytest --cov=src --cov-report=term-missing
    
## Результаты тестов

<img width="1155" height="244" alt="Снимок экрана 2026-06-13 в 04 53 24" src="https://github.com/user-attachments/assets/ab01a963-1e63-4d34-baef-b2aa82a908c6" />


## Запуск программы
    python -m src.db
    python3 -m src.db
